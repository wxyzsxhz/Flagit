# Base URL of the backend API (see ../backend). Defaults to
# http://localhost:4000/api if unset — only needed if the API runs
# somewhere else (a different port, a deployed URL, etc).
VITE_API_URL=http://localhost:4000/api
